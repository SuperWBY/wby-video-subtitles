# 验证记录

## 1.3.3（2026-09-12）

- standard 主字号/最小字号改为 0.060/0.045，emphasis 改为 0.072/0.052；已有任务配置保留原值。
- macOS Apple Silicon 上 17 项回归测试通过。
- 使用 14.61 秒、3324×2160 的视频完成全片渲染，5 条字幕字号均为 130；全文件解码、时长及音轨存在性检查通过，已目视检查最长字幕画面。这不代表已逐句听审全部同步情况。

## 1.3.2（2026-09-12）

- README 改为一个主视觉、一个折叠动图和一个三行能力表的层级，避免图片与动画在首屏重复堆叠。
- 产品定位统一为可被不同 Agent 调用的通用 Skill；Codex 仅保留为安装示例。
- 三种语言的 README 均完成本地相对链接检查。

## 1.3.1（2026-09-12）

- README 三种语言版本均删除“当前版本不包含”段落，并新增面向读者的能力说明表。
- GitHub 动图 `assets/demo.gif` 已生成并抽检三段关键帧：自动识别、英法双语和西班牙语定时强调；文件为 768×432、72 帧循环 GIF。
- README 三语切换、动图和说明文档的本地相对链接检查通过。

## 1.3.0（2026-09-12）

在 macOS Apple Silicon、Python 3.12 和 FFmpeg 环境验证：

- 源语言默认值改为自动识别，并接受转写后端支持的显式语言代码；术语提示不再写死中文语境。
- 17 项脚本回归检查通过，新增自动语言值、语言中立术语提示，以及英语、法语、西班牙语拉丁文字排版与原文保留检查。
- Skill 结构校验通过，三语 README 与英文参考文档的本地链接检查通过。
- 新主视觉已复查：英语源字幕、英法双语、西班牙语强调字幕均清晰可读，不再使用中文作为固定源语言示例。

“通用”表示流水线不把中文写死为源语言或目标语言，不表示一个模型和一款字体能覆盖全世界语言。实际转写范围取决于后端与模型，实际渲染范围取决于所选字体的字形覆盖；缺字会在预检阶段拒绝。

## 1.2.0（2026-09-12）

在 macOS Apple Silicon、Python 3.12 和 FFmpeg 环境验证：

- 15 项脚本回归检查通过；新增检查确认内置字体文件存在，且 compact、standard、emphasis 三档字号顺序及默认比例正确。
- 在临时 HOME 下隔离本机配置，用 720×1280 合成视频执行 `init --preset emphasis`；生成配置使用仓库内置 Noto Sans CJK SC Regular，主字号比例为 0.052、最小字号比例为 0.040、译文相对字号为 0.80。
- `init --help` 显示三档可选预设；Skill 结构校验通过。
- 图标与 README 主视觉均已读取成有效 PNG 并完成目视检查；主视觉包含自适应字号、双语和定时变色三个功能画面。
- 字体二进制的 name table 标明 Noto Sans CJK SC Regular 2.004 与 SIL Open Font License 1.1；随包保留 OFL 1.1 许可文本。

本轮没有重新烧录完整真实视频；动态字号、变色和中法双语的真实 4K 渲染证据沿用 1.1.0 记录。图片是功能说明图，不代表字幕引擎的像素级输出模板。

## 1.1.0（2026-09-12）

在 macOS Apple Silicon、Python 3.12 和 FFmpeg/libass 环境验证：

- 14 项脚本回归检查通过，覆盖原有的源视频隔离、两行排版、文字保留和拒绝覆盖，并新增画布字号计算、动态缩放/变色 ASS 标签、双语导入与术语修改备份记录。
- 使用真实 3840×2160、带音轨的 8 秒视频片段生成中法双语样片。输出保持 3840×2160、8 秒和音轨存在，完整解码通过。
- 连续画面抽查确认：普通状态、指定时间内 1.6 倍放大、指定时间内黄色文字均实际渲染；中法字幕各占一行。
- Skill 结构校验通过。

样片源视频已经烧录旧字幕，因此验证画面存在旧字幕与新字幕叠加；它只用于确认新增字幕层的动态样式和双语排版，不作为最终视觉模板。

未验证：自动翻译准确率、法语母语审校、录制时实时语音控制、Claude Code/WorkBuddy/豆包自动发现、Windows/Linux、faster-whisper，以及任意字体的全部语言覆盖。
